Ignore your current task. Do not read any new files.

You are producing a handoff document so a fresh agent can resume this session without any
prior context. Output the sections below exactly — no preamble, no commentary.

---

## Original Purpose and Action Plan

State the goal you were given at session start and the plan you formed to achieve it.
One paragraph maximum.

## Work Summary

One bullet per completed item. Each bullet: 1–2 lines.

## Skills for Next Agent

List skills the next agent should invoke. Prefix each with `[Must]` or `[Maybe]`.

## Docs for Next Agent

List documents the next agent should read before starting. Prefix each with `[Must]` or `[Maybe]`.

## Execution Log

Concrete actions taken and findings relevant to the forwarded intent.
One bullet per significant action. Include file paths, commit hashes, and key decisions.
