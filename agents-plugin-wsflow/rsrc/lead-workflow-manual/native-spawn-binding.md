### Delegated playbook bindings

`playbook.render` returns the rendered prompt path first, followed by the
host-neutral `recommended-tier`. It may also return concrete native binding
metadata when the current harness and local configuration resolve it. Keep the
tier as the portable delegation vocabulary; follow the current harness's
guidance for any concrete binding fields.
