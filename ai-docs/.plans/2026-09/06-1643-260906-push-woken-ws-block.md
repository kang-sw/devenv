# Plan: 260906-bug-ws-pi-goal-reminder-races-child-push-at-settle — Phase 2: Carry the ws block on push-woken runs

## Relevant Ticket Contract
- Implement selected Phase 2 only: idle custom pushes must not start runs directly. Hold every idle push in the existing FIFO; issue one one-line `sendUserMessage` wake naming the number waiting; confirm the run at `agent_start`, then flush using each recorded `deliverAs`. The user wake goes through `prompt()` / `before_agent_start`, retaining a fresh ws block throughout that run.
- Generalize Phase 1's reminder-start guard into a shared wake-start-pending guard. One pending start covers both push and reminder arrivals; retain held pushes until actual start. Clear at `agent_start`, `agent_settled`, or timeout. If a wake never starts, timeout retries while pushes remain held, including outside goal mode. No `nextTurn` delivery or turn-less custom append as a shortcut.
- Compose hold reasons: compaction prevents ALL release, including an `agent_start` fired before deferred compaction release. Keep the existing settle-time followUp boundary, FIFO order, flush-time status rebuild, and normal streaming steer behavior. `injectDiscussionSummary` uses the same delivery path without delaying its thread-close/stop/persistence side effects.
- Preserve reminder model-facing content and runaway accounting: only dispatched goal reminders advance the existing reducer; a push wake is not a reminder. Push customType/content/display/details/rendering remain unchanged; only the one-line wake is new.
- Preserve landed async-explore integration (`21488404`, subsequent oneShot cleanup fixes): lead explore is a oneShot RPC child in the shared registry and DOES wake the lead. The selected ticket's old separate-registry/non-waker prose is stale and expressly superseded by the lead's instruction.
- Preserve verbatim carry (`bfcf850b`, spec `f873268f`): raw payload (including empty/Unicode/whitespace) survives push wakes, busy release, timer cancellation, and yields; only the next eligible goal-reminder dispatch returning consumes it. A push wake must neither include nor consume carry. Goal replacement, terminal levers, force-stop, shutdown retain their discard semantics.
- Adapter runtime edits only in `agents-plugin-pi/`; no ws-mcp or installed Pi edits. Migration anchors 260605 and 260802 preserve adapter → harness-neutral ws-mcp dependency, no mercenary changes or resurrection, and existing role/registry containment. Accepted local implementation choices are autonomous; only unsettled binding decisions or Always-ask changes stop.
- Spec closeout: update `ai-docs/spec/pi-adapter-runtime.md` report-channel and goal-loop guard/delivery passages plus lead-bootstrap freshness sentence. Reconcile directly affected compaction ordering prose without changing carry-forward policy. Live acceptance remains owner-run.

## Out of Scope
- Reimplementing Phase 1 timer/config/backstop, changing delay defaults, changing goal policy or model-facing reminders, redesigning compaction, retry/approval policy, or fixing Pi's upstream failed-start/finally bug.
- New `lead-wake.ts` serializer, separate push FIFO, synthetic polling for lead explore, `nextTurn` or custom-append fallback, bypassing bootstrap via payload/system-prompt patches.
- Changing worker-owned one-shot explore, oneShot lifecycle/sidecar exclusions, spawn APIs, shared ws/wsflow prompts, tool exposure, registry persistence, TUI rendering, manual snapshot policy, or skill cache semantics.
- Unrelated static-body-cut work and its existing plan `ai-docs/.plans/2026-09/06-1203-260906-bug-ws-pi-workflow-manual-static-body-cut-never-matches.md`: leave untouched.
- Owner live gates are not fabricated from unit tests; do not close the ticket as fully accepted merely because source verification passes.

## Codebase Findings
1. `agents-plugin-pi/src/spawner.ts#L1240-L1405` — existing `leadIdleRef`, `leadCompactingRef`, reminder guard, and mixed `HeldPush | HeldRawSend` FIFO are the reuse seam. Current raw closure lacks explicit delivery metadata; current undefined idle accessor intentionally sends through. Extend this mechanism, do not duplicate it. Production startup supplies the accessor; tests must initialize lifecycle state rather than relying on an unsafe direct-start default.
2. `agents-plugin-pi/src/spawner.ts#L1414-L1571` — `sendPush` rebuilds status and preserves renderer metadata. `flushHeldPushes` currently splices the entire queue unconditionally, then its first custom send starts an idle run. `registerPushFlush` only listens at settle; `pushToLead` currently holds busy followUps and compacting steers. Merely adding an idle predicate would deadlock: idle flush must request a user wake WITHOUT draining, while confirmed-start flush must bypass the ordinary busy-followUp hold.
3. `agents-plugin-pi/src/goal-loop.ts#L500-L645,#L714-L802` — injectable clock, sole `fireReminder`, settle callback, and deferred release already exist. Guard fallback currently belongs only to reminder dispatch; `armSettleTimer` cancels it. Sharing the guard requires coordinating timer ownership so an ordinary settle/compaction release cannot cancel recovery for held pushes. Preserve the exact carry append/consume point at L617-L624.
4. `agents-plugin-pi/src/goal-loop.ts#L828-L975` — goal listeners clear the guard before goal/child checks; `agent_start` currently ALSO clears the compaction flag unconditionally. This conflicts with Phase 2's explicit compose-all-reasons gate: listener order alone cannot fix it. Stop interpreting start as proof compaction release finished; keep deferred `session_compact`/`session_compact_failed` release and lever callbacks authoritative. Existing tests asserting the old unconditional clear need a justified contract update, not blanket deletion.
5. `agents-plugin-pi/src/ask.ts#L958-L995` — summary injection independently chooses immediate custom send versus raw FIFO closure. Route it through the same hold/wake helper as family pushes, with explicit `followUp` mode; leave the following thread/respondent transitions immediate. Do not keep a second send path that still starts an idle custom run.
6. `agents-plugin-pi/src/index.ts#L297-L322,#L349-L356,#L516-L540` — factory registration currently orders goal loop before push flush; session startup installs idle accessor and shared RPC registry; shutdown drops queue and resets goal state/refs. Wire generalized wake lifecycle here once, not inside repeated startup listeners. Forks receive pushes too, while their goal loop remains disabled: push waking must work for lead/fork without arming goal timers in worker/explore processes.
7. `agents-plugin-pi/src/lead-bootstrap.ts#L127-L166,#L263-L282` — existing hook builds manual snapshot + guide + live path-keyed skills block additively. Reuse it unchanged rather than duplicating ws injection. Fresh means current discovered skill paths for this run, not a refetch of the manual snapshot on every wake.
8. `agents-plugin-pi/src/spawner.ts#L3130-L3183` and `agents-plugin-pi/src/index.ts#L355-L356` — lead `explore` calls `spawnAgent(rpcRegistry, { oneShot: true, spawnRole: "explore", ... })`. Its ordinary pushed completion and fan-in behavior must stay intact; no async-explore exclusion in the goal gate. Existing oneShot tests in `agents-plugin-pi/test/spawner.test.ts` cover launch-failure deletion, stop deletion, and tool wiring.
9. `agents-plugin-pi/test/goal-loop.test.ts#L542-L690` — reusable fake clock enforces a single active timer, but fake `on` uses a Map that overwrites same-event handlers and fake user send does not actually run bootstrap/start. New cross-module tests need ordered handler arrays and explicit preflight/streaming/start/settle phases; simply registering all modules into this existing fake would silently omit listeners. Carry regressions start at L682; existing settle mechanics start at L1321. Other focused suites: `test/spawner.test.ts`, `test/ask.test.ts`, `test/lead-bootstrap.test.ts`.
10. `agents-plugin-pi/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js#L772-L786,#L821-L949,#L1099-L1134` — installed runtime confirms user prompt invokes bootstrap and sets `_systemPromptOverride`; idle triggering custom send goes straight to `_runAgentPrompt`; streaming `triggerTurn: true` uses steer/followUp queues, while false takes a distinct deferred-custom path. Therefore flush at confirmed streaming start with normal recorded mode, not false. `prepareNextTurnWithContext` at L293-L302 uses override-or-base, explaining multi-call block loss.

Survey references read: `impl-playbook`; root orientation (no nested ai-docs AGENTS ancestors found); `ai-docs/mental-model/named-agent-runtime.md` and `workflow-skills.md`; both migration anchors. These mental models are shared/historical orientation, not the Pi delivery implementation. Applicable installed Pi docs read completely: `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/docs/{extensions,compaction,skills,session-format,tui}.md`, plus the linked `examples/extensions/send-user-message.ts`. No provider/RPC protocol or custom-component API change is planned.

## Implementation Plan
1. **Tests first for lifecycle logic.** In `agents-plugin-pi/test/spawner.test.ts` and/or a focused new `test/push-wake.test.ts`, extend existing test patterns with an injectable clock and ordered event-handler arrays. Model user-send preflight separately from confirmed streaming start, including no-event rejection/handled input. Add the acceptance matrix below before implementing the state transitions; use existing bootstrap handler rather than a mock ws-block string composer.
2. **Unify queue admission/release in `agents-plugin-pi/src/spawner.ts`.** Keep existing FIFO and build-at-release family rendering. Give raw sends explicit delivery metadata and expose one lower-layer helper usable by `ask.ts`. Hold all compacting/idle/pending-start sends; preserve ordinary busy followUp holding and immediate busy steer delivery. On idle flush/admission, retain the queue and request one user wake; on confirmed `agent_start`, release eligible queued pushes with recorded modes even though busy followUp admission would normally hold. Snapshot/drain only after eligibility is established; do not re-enqueue this same batch via the normal admission path. Keep undefined/torn-down pi/role handling explicit and avoid a production direct-custom-start fallback.
3. **Generalize the existing guard, not a second serializer.** Keep shared pending-start state and start-request helpers with the existing spawner queue mechanism, wired by factory-scope registration in `agents-plugin-pi/src/index.ts`. Reuse `readGoalLoopConfig`/`resolveSettleDelayMs` and injectable scheduler defaults (unref, bounded timer count). A synchronous guard reservation precedes `sendUserMessage(..., {deliverAs:"followUp"})`; subsequent arrivals join the held queue. Recovery must be armed even for synchronous send failure and cannot depend on a goal being active. Guard timeout clears/re-evaluates: held pushes get another counted one-line wake once idle and not compacting; without pushes, existing active-goal reminder retry remains settle-timer-owned. Clear/reset on start/settle/shutdown. Factory registration allocates no running timers; worker/explore roles never schedule them. Reversible naming/wiring details are executor choices.
4. **Integrate reminders and compaction in `agents-plugin-pi/src/goal-loop.ts`.** Route `fireReminder` through the shared reservation, with held pushes/pending wake taking priority BEFORE reducer advancement or pending-origin consumption. A reminder already reserved when a push arrives is the one wake; the push stays held for its start. A push already reserved makes the reminder yield. Do not let settle arming cancel a live push-start timeout. Preserve all goal timer cancel points, swallowed-settle replay, failure wording, and carry lifetime. Reconcile the unconditional `agent_start` compaction backstop with the accepted hold rule: never clear a still-in-flight hold just to flush. Deferred release clears the compaction reason; idle release requests wake without draining, while existing busy-release behavior may leave followUps to that turn's settle. Add a regression where release arrives after start, proving no early flush and eventual delivery. Do not introduce a broad compaction-state redesign.
5. **Use the same mechanism in `agents-plugin-pi/src/ask.ts`; finish lifecycle glue in `src/index.ts`.** Remove obsolete `composeLeadTurnStartOptions` / reminder-only names or adapt all callers atomically; no raw summary closure may retain the old false-append branch. Keep summary side effects, queue-drop shutdown policy, live registry assignment, and bootstrap composition. Recheck all direct lead `sendMessage` call sites by focused search; custom rendering and unrelated user commands stay unchanged.
6. **Documentation closeout after source verification.** In `ai-docs/spec/pi-adapter-runtime.md`, amend `{#260904-pi-report-to-lead-channel}`, `{#260904-pi-goal-loop-arming-settled-levers}`, and `{#260905-pi-lead-bootstrap-system-prompt}` for idle hold, counted wake, shared pending flag, recovery, compose-all-reasons gate, and start flush; reconcile directly affected compaction-release/backstop sentences under `{#260904-pi-goal-loop-model-driven-compaction}` without altering its verbatim-carry contract. Load spec conventions before edits. If the lead requests ticket Result/live-gate closeout, use only the narrow read authority in Escalations; append actual verification and pending owner checks without rewriting frozen Phase 1 results or marking owner-live acceptance complete.

## Verification Plan
- **Unit/fake-clock matrix:** idle followUp and steer each remain held before confirmed start; exactly one user wake with waiting count; actual registered bootstrap hook fires; start flush retains content/details/display/mode; two pushes in the same tick (both arrival orders/mixed modes) yield one wake and two deliveries; reminder/push in both orders yields one start and no extra streak advance; timer retry retains all pushes after handled-input/no-event preflight failure and synchronous throw; retry works with goal inactive and on a fork owner; pending wake cannot be cancelled into a dead queue by settle/compaction release; shutdown clears timers/refs/queue; no child timers.
- **Compaction matrix:** start while compacting flushes nothing even with real factory listener order; completion/failure callback/event clears the reason and eventually delivers once; busy release preserves settle-time delivery; manual and threshold/overflow paths retain swallowed-settle recovery. Ordinary busy followUps still wait for settle and rebuild current registry status; busy steers remain immediate. Summary injection follows all the same hold/wake boundaries while thread side effects remain immediate.
- **Bootstrap regression:** exercise `registerLeadBootstrap` in the lifecycle harness with a changed live skill-path set before the push wake; verify the run's override contains manual snapshot, guide, and new skills through at least two simulated model calls. This is wiring evidence, not proof of real provider payload lifetime.
- **Preservation:** run all existing carry-forward tests (raw exact/empty string, success/failure, busy owner start, yields, cancel/disarm/shutdown); add push-wake-between-compaction-and-reminder case proving carry is untouched until the reminder. Keep shared-registry oneShot explore fan-in and completion/cleanup tests green; include an explore completion as the idle push source. Do not reinstate stale separate-registry expectations.
- Commands from repo root: `cd agents-plugin-pi && node --test test/goal-loop.test.ts test/spawner.test.ts test/ask.test.ts test/lead-bootstrap.test.ts` (include any new focused file); then `cd agents-plugin-pi && npm test`. Read full output. `agents-plugin-pi/package.json` has no separate build/typecheck script; do not invent one or use packaging/skill regeneration as a substitute. Run `git diff --check` and inspect explicit changed paths; preserve unrelated static-body-cut plan. Survey itself ran no implementation tests.
- **Owner live gate (pending, not automatable acceptance):** fresh Pi session, child push wakes an idle lead; require another model call in that same run and inspect actual model/system context for the manual block and fresh skill list. Also retain the prior Phase 1 pending drain race check: child and lead settle together, no `already processing` error, Esc still interrupts, settling status visible. Unit tests cannot claim either live gate passed; record owner evidence when supplied, otherwise report pending.

## Escalations
- Research escalation: **None**. Confidence: **high**. Accepted behavior is explicit and existing FIFO/bootstrap/timer mechanisms are reusable. Shared guard ownership and compaction listener ordering are implementation risks, not unsettled product design.
- **Lead-granted narrow ticket-read authority:** the implementer is explicitly authorized to read the FULL selected ticket `ai-docs/tickets/ready/260906-bug-ws-pi-goal-reminder-races-child-push-at-settle.md` **ONLY at lead-requested Result/live-gate documentation closeout**. This does NOT authorize ticket reads during source implementation or expansion into other phases. This is a read-authority exception, not an unresolved design/research escalation.
- Correct known stale premises on contact within closeout scope: the ticket's async-explore non-waker description no longer matches landed source; Phase 1's unconditional start-time compaction-clear/backstop and direct custom-start descriptions are superseded only where Phase 2 requires compose-all-reasons delivery. The historical named-agent mental model describes ws mercenaries, not the Pi shared registry. No additional spec/doc assumption was used to override source.
- Stop and report structural gaps or genuinely unsettled binding/Always-ask changes; choose reversible local implementation details autonomously. Owner-live acceptance remains a reported limit, not a source-execution blocker.
