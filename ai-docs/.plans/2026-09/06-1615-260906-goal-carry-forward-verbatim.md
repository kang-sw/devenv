# Plan: 260906-bug-ws-pi-goal-loop-reinject-races-manual-compaction — Phase 2: Carry the lever's string forward verbatim

## Relevant Ticket Contract
- Capture the exact `carry_forward` string before the lever calls `ctx.compact`; continue passing it unchanged as Pi's `customInstructions`.
- Include the string once in the next dispatched goal reminder, on success or failure, under `Carried forward verbatim from before compaction:`. No trimming, escaping, indentation, summarization, or newline normalization of the payload. No pending payload means no heading; consume only when sent.
- Keep the lever non-terminal. Preserve the landed pure settle reducer, compaction hold/release, swallowed-settle replay, delayed reminder, child-yield gate, and boundary guard. No sends inside compaction event handlers.
- Amend `ai-docs/spec/pi-adapter-runtime.md`'s model-driven-compaction lever passage. Live verification remains owner-run.
- Adapter-local runtime work only (`agents-plugin-pi/`); the migration anchor and AGENTS boundary exclude ws-mcp host-specific logic or changes to shared workflow semantics.

## Out of Scope
- Reimplementing Phase 1, changing timers/streak accounting/push order, shadowing `/compact`, or closing accepted non-lever compaction windows.
- Durable carry-forward persistence across reload/session replacement, replacing Pi's summarizer, new config/schema requirements, or changing the public lever argument.
- Other tickets, packaging/version changes, and the unrelated untracked static-body-cut plan.

## Codebase Findings
- `agents-plugin-pi/src/goal-loop.ts#L995-L1048` — lever already passes `p.carry_forward` unchanged to `customInstructions` and tool-result text, but stores no copy for the reminder. Capture before `ctx.compact`, not in its callbacks.
- `agents-plugin-pi/src/goal-loop.ts#L259-L282` and `#L355-L389` — `GoalLoopState` has pure fresh arm/disarm constructors; tool-call and reinject transitions spread existing state. An optional pending carry field fits this existing lifetime: waiting/yield preserve it, fresh goal/disarm/force-stop discard it.
- `agents-plugin-pi/src/goal-loop.ts#L477-L530` and `#L706-L768` — `pendingRearm` and its failure reason survive timer delays, but the timer gates on current compacting/idle/child state before dispatch. Do not consume carry at event receipt, release, timer arm, or a yielding timer fire.
- `agents-plugin-pi/src/goal-loop.ts#L611-L645` and `#L650-L689` — `fireReminder` is the single send path for lever and ordinary/replayed reminders, using explicit `followUp` plus the boundary guard. It is the reuse point for appending and consuming carry regardless of reminder origin; no new sender is needed.
- `agents-plugin-pi/src/goal-loop.ts#L796-L813`, `#L907-L926`, and `#L1052-L1064` — busy release and `agent_start` backstop clear orchestration markers without sending; shutdown resets instance state. Risk: tying carry lifetime to `pendingRearm` would lose it on busy handoff. Keep carry independent of those marker clears, but clear it on shutdown and goal replacement/termination.
- `agents-plugin-pi/test/goal-loop.test.ts#L542-L677` — reusable `fakeClock`, `fakePi`, and `fakeCtx` harness captures send contents/options and compaction callbacks. `#L679-L758` covers deferred/idempotent success and error release; `#L803-L840` covers busy release followed by ordinary settle. Extend these patterns rather than adding a second harness or live-model unit tests.
- `ai-docs/spec/pi-adapter-runtime.md#L1285-L1293` — lever passage promises only summary steering today. Its following re-arm prose (`#L1294-L1334`) still describes direct release-time sends, whereas current code arms a timer. Do not copy that stale timing into implementation; narrowly reconcile the touched passage with the existing delayed-reminder contract rather than changing orchestration.
- `/opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent/dist/core/compaction/compaction.js#L488-L494` — Pi appends custom instructions as `Additional focus`, confirming they cannot guarantee verbatim model-visible carry. Installed `docs/extensions.md`, `docs/compaction.md`, and `docs/session-format.md` were read completely, with relevant `trigger-compact.ts`, `send-user-message.ts`, and `custom-compaction.ts` examples: callbacks are the completion mechanism, user messages enter model context, and custom persistence entries alone do not.

## Implementation Plan
1. In `agents-plugin-pi/test/goal-loop.test.ts`, add focused red tests using the existing fake clock and IO harness for exact payload inclusion, absence, and one-shot clearing. Use a distinctive multiline Unicode string with leading/trailing spaces, tabs, and CRLF so normalization mistakes are observable.
2. In `agents-plugin-pi/src/goal-loop.ts`, add an optional pending carry string to `GoalLoopState` and capture `p.carry_forward` before `ctx.compact`. Rationale: goal-scoped state survives ordinary reducer transitions while fresh arm/disarm/force-stop constructors prevent cross-goal leakage. Do not alter reducer decisions or release markers.
3. In `agents-plugin-pi/src/goal-loop.ts`, append `\n\nCarried forward verbatim from before compaction:\n` plus the raw payload in the shared `fireReminder` path (a small pure formatting helper is appropriate for direct unit coverage). Check presence with `!== undefined`, not truthiness: the existing schema accepts an empty string. Clear the captured payload after `pi.sendUserMessage` returns, before scheduling the fallback timer; a synchronous send throw must not consume it. Preserve the existing failure prefix, followUp option, and boundary-guard logic. No delivery-ack/retry protocol is introduced by this phase.
4. In `agents-plugin-pi/src/goal-loop.ts`, explicitly discard pending carry in `resetCompactionStateForShutdown`; rely on fresh state for goal replacement and terminal/force-stop cleanup. Leave carry untouched by busy release, backstop flag reset, timer cancellation, and yield. Update only directly affected lever descriptions/comments to explain summary steering plus verbatim reminder delivery.
5. In `agents-plugin-pi/test/goal-loop.test.ts`, cover success via deferred `session_compact`, callback-only success, both completion sources together (one reminder), callback-only failure and failed-event release (failure reason plus exact carry), no carry/no heading, second reminder/no carry, empty payload, and capture-before-compact ordering. Extend busy-release and agent-start-backstop cases to prove an ordinary later reminder still carries the string. Cover intervening child/idle yield, new goal, terminal lever, and shutdown so payload never leaks or disappears before its eligible send.
6. In `ai-docs/spec/pi-adapter-runtime.md`, amend the lever passage under `{#260904-pi-goal-loop-model-driven-compaction}` with exact heading, one-shot success/failure guarantee, unchanged custom instructions, and lifecycle boundary. Phrase delivery as the next eligible dispatched reminder through the existing delayed settle path, not necessarily the first arbitrary message after compaction (held pushes/owner input retain priority). Read spec conventions before editing. Narrowly correct the adjacent direct-send wording if touched; do not broaden into Phase 1 redesign.

## Verification Plan
- Focused implementation check: `cd agents-plugin-pi && node --test test/goal-loop.test.ts`.
- Regression check: `cd agents-plugin-pi && npm test` (package has no build script). Read full output; retain all existing reducer, release, timer, push, and boundary-guard assertions.
- Exactness assertions should compare the suffix after the fixed heading with the original string using strict equality, plus one heading occurrence and no heading in the following reminder. Verify unchanged `customInstructions` independently of tool-result echo.
- Confirm no reminder synchronously inside `session_compact`; fire deferred release then fake settle timer explicitly. Busy/yield paths must send nothing until a subsequent eligible settle. A released push must still precede the reminder.
- Owner-run live gate in a fresh Pi session with enough context: call the lever with a distinctive sentence; inspect the first goal-reminder message after compaction and compare its payload byte-for-byte. In an otherwise quiet run this is the first post-compaction message. Do not claim model paraphrase or a TUI notification proves the guarantee. Prior Phase 1 dogfood checks remain pending, not reclassified as complete.
- Plan-only survey verification: check all six headings, cited paths, scoped diff, and unchanged unrelated files. No runtime tests are required for this plan-only artifact; implementation tests above have not been run by the survey.

## Escalations
- None. Confidence: high; eight meaningful findings.
- Lead-authorized closeout: the implementer may read the full target ticket `ai-docs/tickets/ready/260906-bug-ws-pi-goal-loop-reinject-races-manual-compaction.md` solely for Phase 2 Result documentation and assessment of binding owner-run live-check gates and any dated Blocked note. Source, review, and spec work are already landed; source scope is unchanged. This authorization does not waive live-check gates or authorize claiming unperformed checks as complete.
- Main shortcut risk is clearing carry with `pendingRearm`, which current busy/backstop paths intentionally discard. Goal-scoped carry plus common-send consumption avoids changing settled orchestration.
- Documentation discrepancy: adjacent spec re-arm prose predates landed timer orchestration. Source and current timer tests govern timing; no mental-model entry supplied a wrong implementation assumption. Inventory has no Pi-specific mental model; `plugin-runtime.md` and `workflow-skills.md` supplied the relevant ancestor boundaries.
