# Plan: ticket.verify + commit-gate mechanical backstop, then must-not-forget mutation-tool collapse — Phase 2: must-not-forget mutation-tool collapse + action-time obligation prose

## Relevant Ticket Contract

- Seed classification for the must-not-forget filter: ready→sage-posture is **HARD** (keep thin tool); close→phases-resolved is **SOFT** (free to verify-warn, no hard block). The ticket requires the **complete** per-tool disposition, not only these two seeds.
- Rename `tickets.create` → an attention-salient name (ticket gives illustrative examples only: `create_empty` / `create_template`); return prose must state it yields a "valid empty skeleton + initial posture", not a full mutation orchestrator.
- Free `tickets.close` to free-edit + soft verify-warn on unresolved phases (no hard block).
- Keep the ready-move hard enforcement (sage posture) unchanged — the catastrophic end of the filter.
- Add a thin **lead-only** `sage.stamp` tool; reviewers stop writing frontmatter. It writes `completed`/`blocked` posture + renders the `## Blocked` companion — stated as "the thin, explicitly-named lead-only replacement for `sage_record`'s write," preserving the lead-single-writer property already established by the `sage-gate-record-tools` spec (`{#260720-sage-gate-record-tools}`).
- Replace front-loaded playbook procedure with **action-time obligation prose**: each tool/gate return surfaces the must-know follow-on for that action, instead of restating all obligations up front in the playbook.
- Acceptance check (verbatim from ticket): renamed create tool's return prose states the "valid empty skeleton + initial posture" caveat; `tickets.close` on a ticket with unresolved phases soft-warns without blocking; a ready move still hard-blocks on missing/blocked sage posture; `sage.stamp` writes the posture + `## Blocked` companion and is exercised only via the lead path (reviewers no longer write frontmatter); the write-ticket path's front-loaded procedure prose is measurably reduced (net token count on base path drops). Go tests cover the close soft-warn and ready hard-block cases.
- Import-boundary invariant `{#260720-wsdoc-commit-boundary}` still applies: `internal/wsdoc` must never import `internal/wsgit`; a wsdoc mutation function computes the frontmatter write + commit inputs (title, paths, ai_context) and returns them, and the `callTool` MCP dispatch case performs the actual `wsgit.NewClient().Commit(...)`. `tickets.sage_gate` (ask-decline path) and `tickets.sage_record` are the existing reference implementations of this pattern; `sage.stamp` must follow it identically.
- Phase 1 is landed (commits `4a124fca..97444293`, `9744429` merged): `wsdoc.TicketVerify(root, paths)` exists with hard guardrails (stem, status-dir, file-exists, frontmatter-fence, ready-sage-posture via shared `readyPostureProblems`, close-date-field, phase/Result heading well-formedness) plus a soft spec-address warning; wired as the `git.commit` gate via a `Verifier` seam on `wsgit.Client`. Phase 2 builds on this floor and must reuse `readyPostureProblems`/`TicketVerify`'s warning mechanism rather than re-scattering logic ("single source of truth", ticket L99-102, already established in Phase 1 Result).

## Out of Scope

- Promoting spec-address from soft-warn to a hard gate — owned by `260723-feat-ready-spec-address-hard-gate` (blocked on the collocator), explicitly deferred by the parent ticket.
- Append-only phase/Result diff enforcement — a diff-level property `TicketVerify`'s snapshot check does not attempt (already settled in Phase 1).
- `tickets.template` / `tickets.checklist` — pure lookup tools with no mutation, no `session_key`, no catastrophic follow-on; the must-not-forget filter does not apply to them.
- `tickets.list` / `tickets.find` / `tickets.status` — read-only discovery tools, not mutation tools; out of filter scope.
- `tickets.sage_gate` — Phase 2 does not rename or restructure it; only `tickets.sage_record`'s write is replaced by `sage.stamp`. `sage_gate`'s own frontmatter writes (`resolveConcretePosture`'s config-fallback stamp, the ask-decline `skipped` stamp) are provisional/idempotent config-driven writes, not the review-verdict-driven terminal write that motivates the lead-single-writer property; leave `sage_gate` behavior and name unchanged.
- The adjacent `idea/` bug `260721-bug-lead-write-ticket-sage-ready-ordering` (Move-before-Sage-Gate ordering in `lead-write-ticket.md`'s `## On: Move` / `## On: Sage Review Gate`) — not referenced by this ticket; do not fix it opportunistically while rewriting the same file, but do not make the ordering worse either.
- `ai-docs/tickets/todo/260710-bug-lead-write-ticket-playbook-runtime-schema-drift.md` — already resolved (Result recorded `24fd055`); its golden-test drift guard (`TestPlaybookPrintGoldenLeadWriteTicket`) is a coupling to preserve, not a task to redo.

## Codebase Findings

### Complete per-tool must-not-forget disposition (all ticket mutation tools)

| Tool | Disposition | Evidence |
|---|---|---|
| `tickets.create` | **KEEP (thin), RENAMED**. Retains the never-skippable design-review hard block for direct-to-`ready/` creation (same catastrophic class as ready-move) — this check is NOT currently routed through the shared `readyPostureProblems` predicate; it's an inline check. | `agents-plugin-tool/internal/wsdoc/ticket_create.go#L51-62` (inline `designRequired && resolved != "completed" && resolved != "skipped"` block, does not call `readyPostureProblems`) |
| `tickets.close` | **FREE**. No existing phase-related check (hard or soft) exists anywhere today — this is genuinely new guardrail work, not a "loosen an existing hard check" task. Must add a new SOFT warning. | `agents-plugin-tool/internal/wsdoc/tickets_mutate.go#L57-99` (`TicketsClose` — no phase logic at all today) |
| `tickets.move` | **KEEP (thin), HARD, unchanged**. Ready-promotion sage-posture block already routes through the shared `readyPostureProblems` predicate (Phase 1 single-source-of-truth). No code change required by Phase 2; ticket explicitly says "keep." | `agents-plugin-tool/internal/wsdoc/tickets_mutate.go#L101-153` (`TicketsMove`), `#L396-441` (`readyPostureProblems`, shared with `TicketVerify`) |
| `tickets.verify` | **KEEP (Phase 1 output)**, gains a new SOFT "unresolved phases" warning for `.done`/`.dropped` status, mirrored by `tickets.close`'s own tip (same `readyGateWarning`-style pattern already used for spec-address). | `agents-plugin-tool/internal/wsdoc/tickets_verify.go#L28-31` (existing `ticketPhaseHeadingRE`/`ticketResultHeadingRE`), `#L125-130` (close-date-field hard check — the natural sibling location for the new soft check), `#L136-140` (existing `addWarning("spec-address", ...)` — the pattern to mirror) |
| `tickets.sage_gate` | **KEEP, unchanged**. Not named for rename by the ticket; writes only provisional/config-driven postures, not the terminal completed/blocked write. | `agents-plugin-tool/internal/wsdoc/tickets_sage.go#L91-158` |
| `tickets.sage_record` → `sage.stamp` | **KEEP (thin), RENAMED + newly LEAD-GATED**. Today `tickets.sage_record` is **not** in `isLeadOnlyTool` — a delegate-scoped (reviewer) session key can call it right now; the "already the sole frontmatter writer" claim in the ticket's Decisions section is a *playbook-convention* fact (reviewer prompts never call it — confirmed, see below), not a code-enforced one. Adding the lead-only gate is a real, currently-missing enforcement step, not a no-op preservation. | `agents-plugin-tool/internal/mcp/server.go#L55-68` (`isLeadOnlyTool` — no `sage_record`/`sage_gate` entry), `#L4265-4279` (`roleAllowsTool` — delegate blocks only `session.`/`mercenary.`/`config.` prefixes) |

Confirmed zero current call sites of `sage_gate`/`sage_record`/`sage.stamp` inside either reviewer playbook (`agents-plugin/rsrc/ticket-reviewer-design/ticket-reviewer-design.md`, `agents-plugin/rsrc/ticket-reviewer-completeness/ticket-reviewer-completeness.md`) — the lead-only gate is a pure hardening change with no legitimate caller to break.

### Existing action-time-prose precedent (reuse this mechanism)

- `formatSageGate` / `sageGateNextInstruction` already emit a `next_instruction: ...` line naming the exact next call (`agents-plugin-tool/internal/mcp/server.go#L2494-2526`), and `formatSageRecord` does the same (`#L2538-2557`). This is the established pattern for "action-time obligation prose" — Phase 2 should extend it to `tickets.create`/`tickets.close`/`tickets.verify`'s formatters, which today only emit a plain `Tip:`/`tip:` line with no explicit `next_instruction:` (`#L2435-2450`, `#L2478-2492`).
- There is direct precedent for trimming playbook prose in favor of tool-owned text: the `260701` relocation (referenced in `TestPlaybookPrintGoldenLeadWriteTicket`, `agents-plugin-tool/internal/mcp/playbook_tools_test.go#L1921-1940`) already moved the entire sage-review state-machine prose out of `lead-write-ticket.md` into `tickets.sage_gate`/`tickets.sage_record`'s own tool-owned resolution, leaving only a two-step call site plus **forbidden-text** assertions (`"If posture is recommended, ask the user"`, `"Blocked Section Template"`, etc. must NOT appear in the rendered body). Phase 2's prose reduction is the same playbook-diet pattern applied to `tickets.create`/`tickets.close`, not a novel technique.

### `lead-write-ticket.md` surface to change (full text read; 205 lines)

`agents-plugin/rsrc/lead-write-ticket/lead-write-ticket.md`:
- `#L27` — Invariants "Movement" bullet names `tickets.create` by its current name.
- `#L47` — `### 3. Populate` step 1 has the literal call-site string `tickets.create(session_key: <lead key>, stem: "<category>-<name>", initial_state: "<initial-status>")`, asserted byte-for-byte by `playbook_tools_test.go#L1918-1920`.
- `#L66-69` — `### 6. Sage Review Gate` calls `tickets.sage_gate` then `tickets.sage_record`; `#69` is the exact line to change to `sage.stamp`, asserted by `playbook_tools_test.go#L1925-1926` (`"tickets.sage_record(stem, stage, verdicts)"` must currently be present — becomes a **forbidden** string, `"sage.stamp(...)"` becomes the new required string).
- `#L75-80` — `## On: Reviewer Spawn` names `tickets.sage_gate` and hands verdicts "to `tickets.sage_record`" (`#L80`) — update the final hand-off reference.
- `#L82-89` — `## On: Move` names `tickets.close` (unaffected by rename, only the phases-soft-warn behavior changes underneath it).

Two other rsrc files mention `tickets.create` by name (both need the rename applied), confirmed identical in both trees via `diff` (byte-identical, mirror is regenerated, never hand-edited):
- `agents-plugin/rsrc/lead-discuss/lead-discuss.md#L30`
- `agents-plugin/rsrc/lead-sprint/lead-sprint.md#L21`
- Wsflow mirrors at `agents-plugin-wsflow/rsrc/{lead-write-ticket,lead-discuss,lead-sprint}/*.md` are byte-identical today (confirmed via `diff`); **do not hand-edit them** — regenerate via `WS_REGEN_WSFLOW_RSRC=1 go test ./internal/wsrsrc -count=1 -run TestRegenerateWsflowRsrcMirror` after editing the `agents-plugin/rsrc/` originals (`agents-plugin-tool/internal/wsrsrc/wsflow_mirror_test.go#L78-90`). The shipped playbook manifest also needs `WS_REGEN_MANIFEST=1 go test ./internal/wsrsrc -count=1 -run TestRegenerateShippedManifest` (`agents-plugin-tool/internal/wsrsrc/manifest_shipped_test.go#L23-41`).

### MCP/CLI/runtime surface ripple (rename is caller-visible — full list)

For the `tickets.create` rename AND the `tickets.sage_record` → `sage.stamp` change, every one of these needs a matching edit:

1. **Dispatch** — `agents-plugin-tool/internal/mcp/server.go`: `case "tickets.create":` (`#L1163-1185`) and `case "tickets.sage_record":` (`#L1231-1262`) — rename the case string; the `sage.stamp` case additionally needs the `isLeadOnlyTool` gate.
2. **Schema** — same file, `tools()` list: `tickets.create` entry `#L3837-3848`, `tickets.sage_record` entry `#L3885-3897`. Update `"name"` and description prose (create's description must state the "valid empty skeleton + initial posture" caveat per the acceptance check).
3. **Session-key auto-injection allowlist** — `toolSchemaRequiresSessionKey` at `#L4143-4160`, hardcoded name list at `#L4150` includes `"tickets.create"` and `"tickets.sage_record"` literally — **easy to miss**, separate from both `tools()` and `isLeadOnlyTool`.
4. **Lead-only gate** — `isLeadOnlyTool` at `#L55-68` — add the `sage.stamp` name (new addition; `tickets.create` does not need this, it stays open like today).
5. **Formatter** — `formatTicketCreate` (`#L2435-2440`) and `formatSageRecord` (`#L2538-2557`) — reused as-is under new dispatch names unless the return-prose rewrite (action-time obligation) changes their body.
6. **CLI subcommand** — `agents-plugin-tool/cmd/ws-mcp/main.go`: `ticketsCommand` switch `case "create":` (`#L524`), `ticketsUsage()` string (`#L535`), `ticketsCreate()` handler (`#L656-676`, uses label `"tickets create"` in its `printTextOrFatal` call). `tickets.sage_record`/`sage.stamp` has **no** CLI mirror today (confirmed: no `sage-gate`/`sage-record` case in `ticketsCommand`) — do not add one unless a ticket asks; keep the CLI-mirror gap as-is.
7. **`runtimeCapabilityCommandNames()`** — `agents-plugin-tool/cmd/ws-mcp/main.go#L198-241` — `"tickets.create"` at `#L235` (CLI-mirrored tools only; `sage.stamp` is not added here since it has no CLI mirror, matching `sage_gate`/`sage_record`'s current absence).
8. **`agents-plugin/runtime.json`** — `tools` block `#L56` (`tickets.create`) and `#L60` (`tickets.sage_record`); `commands` block `#L108` (`tickets.create` only — sage tools were never in `commands`).
9. **`agents-plugin-wsflow/runtime.json`** — `tools` block `#L57` (`tickets.create`) and `#L61` (`tickets.sage_record`); `commands` block `#L91` (`tickets.create` only). Confirmed wsflow's `tools` block DOES include `sage_gate`/`sage_record` today (unlike `mercenary.*`, they are not agent-backed-hidden tools) — `sage.stamp` must be added to wsflow's `tools` block too, not just full-ws's.
10. **Launcher-contract tests enforce all of the above exactly** — `agents-plugin-tool/cmd/ws-mcp/main_test.go`: `TestRuntimeCapabilitiesCommandReportsWsflowContractSurface` (`#L149-183`) diffs live `runtime capabilities` output against `agents-plugin-wsflow/runtime.json` with `slices.Equal`; the full-ws contract test reads `agents-plugin/runtime.json` via `readRuntimeContractTest` (`#L648-650`). A missed runtime.json edit fails these tests, not a silent drift.
11. **Version.** `runtime.json`'s per-tool ranges (e.g. `">=0.35.1-dev <0.36.0"`) and `plugin_version`/`required_mcp`/`release_tag` are edition points owned exclusively by `agents-plugin-tool/scripts/bump-ws-version.sh` — confirmed the script only rewrites existing version-range values, it does **not** add/rename/remove tool keys. The implementer must hand-edit the tool-name keys themselves as an ordinary source change (rename, add `sage.stamp`, remove `tickets.sage_record`), then separately run the bump script per AGENTS.md's "version bump on dev-merge" rule — these are two distinct steps, not one.

### Go test call sites needing the rename applied

- `agents-plugin-tool/internal/mcp/playbook_tools_test.go#L1918-1933` — `TestPlaybookPrintGoldenLeadWriteTicket`: literal-string assertions for `tickets.create(...)` and `tickets.sage_gate`/`tickets.sage_record` call sites; also a forbidden-string list (`#L1935+`) that the rewritten prose must not reintroduce.
- `agents-plugin-tool/internal/mcp/session_state_test.go#L2163,2199,2235,2288` — 4 call sites invoking `"tickets.create"` via `callToolWithKey`.
- `agents-plugin-tool/internal/mcp/tickets_sage_test.go#L104,125,138` — MCP-level calls to `"tickets.sage_gate"`/`"tickets.sage_record"`.
- `agents-plugin-tool/internal/wsdoc/tickets_sage_test.go` (~25 test funcs) and `agents-plugin-tool/internal/wsdoc/ticket_create_test.go` (10 test funcs) test the Go functions (`wsdoc.SageRecord`, `wsdoc.TicketCreate`) directly, not the MCP tool name string — **unaffected by the MCP-name rename** as long as the Go function names stay `SageRecord`/`TicketCreate` internally (recommended: do not rename the Go symbols, only the dispatched MCP tool-name string, to avoid needlessly touching ~35 passing unit tests).
- New tests needed (no existing coverage): `tickets.close` soft-warn on unresolved phases (mirror `agents-plugin-tool/internal/wsdoc/tickets_mutate_test.go`'s existing `TestTicketsClose*` shape, `#L50-186`, and `tickets_verify_test.go`'s existing warning-guardrail shape, e.g. `TestTicketVerifySpecAddressIsSoftWarnOnly` at `#L261-288`); `sage.stamp` lead-only rejection for a delegate-scope key (mirror any existing `isLeadOnlyTool` rejection test pattern in `internal/mcp`); ready-move hard-block regression (already covered by `TestTicketsMoveUpwardToReadyBlocksUnresolvedSageReviewPosture` etc. in `tickets_mutate_test.go` — confirm it still passes unchanged, per the ticket's explicit "keep ready-move hard enforcement").

## Implementation Plan

1. **`tickets.close` unresolved-phases soft warning** (new mechanism, no existing precedent):
   - Add a pure predicate in `agents-plugin-tool/internal/wsdoc/tickets_verify.go` (near `ticketPhaseHeadingProblems`, reusing `ticketPhaseHeadingRE`/`ticketResultHeadingRE`) that reports, for `.done`/`.dropped` status, any `### Phase N:` heading (not marked `[dropped]`) with no `### Result` heading before the next Phase heading or EOF.
   - Wire it into `verifyTicketFile`'s `status == ".done" || status == ".dropped"` branch (`tickets_verify.go#L125-130`) as `addWarning("unresolved-phases", ...)`, matching the existing `addWarning("spec-address", ...)` shape at `#L136-140`.
   - Give `TicketsClose` (`tickets_mutate.go#L57-99`) its own tip via the same shared predicate, mirroring how `TicketsMove` calls `readyGateWarning` (`tickets_mutate.go#L147-151`) — do not duplicate the phase/Result regex logic a second time.
   - No hard block anywhere in this path.
2. **`tickets.create` rename**: pick a final name consistent with the sibling `tickets.template` lookup tool (recommend `tickets.create_template`, but confirm with the user/lead if `create_empty` reads better — the ticket left this open). Apply the rename across dispatch, schema, `toolSchemaRequiresSessionKey`, CLI subcommand + usage string + handler label, `runtimeCapabilityCommandNames()`, both `runtime.json` `tools`+`commands` blocks, and every rsrc prose reference (`lead-write-ticket.md`, `lead-discuss.md`, `lead-sprint.md`, regenerated wsflow mirror), and every test call site listed above. Rewrite `formatTicketCreate`'s description/return prose (schema description + `Tip:`/`next_instruction:` line) to explicitly state "valid empty skeleton + initial posture, not a full mutation orchestrator" per the acceptance check.
3. **`sage.stamp` addition**: add a new dispatch case reusing `wsdoc.SageRecord` as-is (recommended: do not fork/redesign the Go function or its `(stem, stage, verdicts)` contract — it already satisfies "writes completed/blocked + renders the `## Blocked` companion" and already follows the `{#260720-wsdoc-commit-boundary}` commit-return pattern). Register schema, add to `isLeadOnlyTool`, add to `toolSchemaRequiresSessionKey`, add to both `runtime.json` `tools` blocks (not `commands` — no CLI mirror, matching `sage_gate`/`sage_record` precedent). Remove the `tickets.sage_record` dispatch case, schema entry, and both `runtime.json`/`toolSchemaRequiresSessionKey` entries for it. Update `lead-write-ticket.md`'s Sage Review Gate section (`#L69`) and Reviewer Spawn hand-off (`#L80`) to call `sage.stamp` instead.
4. **Action-time obligation prose**: extend the existing `next_instruction:`-line pattern (`sageGateNextInstruction`/`formatSageRecord`) to `formatTicketCreate`/`formatTicketMutate`/`formatTicketVerify`, then trim the corresponding `lead-write-ticket.md` Invariants/step prose that only restates what the tool response now states directly — mirroring the `260701` sage-review relocation precedent already validated by `playbook_tools_test.go`'s required/forbidden string lists. Report a before/after line or word count of `lead-write-ticket.md` in the Result section as the "measurably reduced" evidence (no existing automated token-budget test to extend).
5. Regenerate the manifest and wsflow rsrc mirror (`WS_REGEN_MANIFEST=1` then `WS_REGEN_WSFLOW_RSRC=1`, both with `-count=1`) after all rsrc edits land.
6. Run `agents-plugin-tool/scripts/bump-ws-version.sh <next-patch>` as the final step (dev-merge version bump rule), never hand-editing the version edition points.

## Verification Plan

- `go build ./... && go vet ./... && go test ./...` from `agents-plugin-tool/` — must stay clean across `internal/wsdoc`, `internal/mcp`, `cmd/ws-mcp`, and `internal/wsrsrc`.
- Targeted: `go test ./internal/wsdoc/... -run 'TicketsClose|TicketVerify|SageRecord|TicketCreate'` and `go test ./internal/mcp/... -run 'PlaybookPrintGoldenLeadWriteTicket|SageGate|SageRecord|TicketsCreate'`.
- `go test ./cmd/ws-mcp/... -run 'RuntimeCapabilities'` to confirm both launcher-contract tests pass against the edited `runtime.json` trees.
- Manual: render `lead-write-ticket` via `playbook.print` and confirm the required/forbidden string sets from the updated golden test, plus a subjective line-count comparison against the pre-change file for the "measurably reduced" claim.

## Escalations

- Confidence: medium
- Reason: the ripple map, per-tool disposition, and mechanical rename obligations are all confirmed by direct code reading (high confidence). Two genuinely open design points remain that the ticket does not settle and that materially shape the diff: (1) the exact final `tickets.create` replacement name — the ticket gives two "e.g." options, not a decision; (2) whether `sage.stamp` reuses `SageRecord`'s existing `(stem, stage, verdicts[])` aggregation contract verbatim (this plan's recommendation, as the clean existing mechanism) or is meant to be a genuinely simplified/redesigned surface, and whether its identifier is truly a new top-level `sage.` namespace (as the ticket repeatedly and literally states, breaking the `tickets.*` grouping every sibling mutation tool uses) versus a `tickets.sage_stamp` naming-convention-consistent alternative the ticket may not have intended to deviate from.
- Research should decide: confirm/finalize the `tickets.create` replacement name; confirm whether `sage.stamp` is a literal rename-in-place of `tickets.sage_record`'s existing contract (recommended default) or requires a narrower/different signature, and confirm the `sage.` top-level namespace is an intentional, permanent convention break (not an oversight) before the implementer touches `isLeadOnlyTool`, `roleAllowsTool` prefix assumptions, and both `runtime.json` trees under that name.
