# Plan: slug-cap-fix

## Relevant Ticket Contract
- Goal: make the `impl/<stem>` branch-slug 15-char limit a RECOMMENDATION, not an enforced hard truncation. `impl/` branches only.
- Edit exactly: `implement_resolver.go` `implementTargetBranchName` (remove truncation, fix doc comment), `implement_resolver_test.go` (the 15-char truncation test case), and three docs (soften wording to "<=15 characters recommended" advisory).
- CRITICAL INVARIANT: `implementTargetBranchName` remains the single shared branch-name constructor; both `deriveImplementBranchPlan` and `handleEnterImplement` (session_state.go) must keep calling it unchanged in call structure — only the truncation logic inside the helper changes.
- Non-goals: `goal/<slug>` in lead-drain-ready-queue, `lead-prefer-subagent`, `lead-implement` skill text — none carry 15-char slug guidance and must not be touched.
- Verification boundary: `go build ./...` and `go test` for the affected mcp package must pass; confirm via grep no other 15-char truncation call site exists for the branch slug. This is a deliberate enforced->advisory semantics change, already user-approved. Version bump happens on dev-merge, not this change.

## Out of Scope
- `ai-docs/mental-model/workflow-skills.md:69` and `ai-docs/spec/workflow-skills.md:458,460` mention `impl/<stem>` but do not state the 15-char cap — leave untouched.
- The `impl/*` auto-delete naming-gate logic (`implement_resolver.go:700`, `strings.HasPrefix(obs.CurrentBranch, "impl/")`) — confirmed prefix-only, not length-based, so it is unaffected by this change and must not be modified.
- `lead-drain-ready-queue`, `lead-prefer-subagent`, `lead-implement` skill docs — explicitly excluded per contract.

## Codebase Findings
- `agents-plugin-tool/internal/mcp/implement_resolver.go#L672-L684` — `implementTargetBranchName`: doc comment (L672-676) says "hard-truncated to 15 characters"; body (L677-682) does `if len(stem) > 15 { stem = stem[:15] }` then `strings.TrimRight(stem, "-")`. This is the sole truncation call site (confirmed by grep for `15` and `TrimRight` across the mcp package — no other hit).
- `agents-plugin-tool/internal/mcp/implement_resolver.go#L686-L687` — `deriveImplementBranchPlan` calls `implementTargetBranchName(n.ScopeSlug)` at L687; this call site is untouched by the fix (helper signature/return type unchanged).
- `agents-plugin-tool/internal/mcp/session_state.go#L1012` — `handleEnterImplement` calls `implementTargetBranchName(normalized.ScopeSlug)`; also untouched, confirms the single-constructor invariant is structural (both call sites already just call the helper, no duplicated logic to unify).
- `agents-plugin-tool/internal/mcp/implement_resolver_test.go#L621-628` — test case `"target branch name is truncated to 15 characters"`: `ScopeSlug: "a-very-long-scope-slug-name"`, expects `wantTargetBranch: "impl/a-very-long-sco"` (truncated to 15 + no trailing dash to trim). Must change to expect the full untruncated stem: `impl/a-very-long-scope-slug-name`.
- `agents-plugin-tool/internal/mcp/implement_resolver_test.go#L629-636` — adjacent test case `"target branch truncation trims trailing dash"`: `ScopeSlug: "abc-defghijklm-nop"` (18 chars), currently expects truncation to `impl/abc-defghijklm` (15 chars, trailing dash trimmed after cut). Contract text names only the first case (~622-627), but this second case is also truncation-dependent and will fail post-fix: without truncation the slug is 18 chars ending in "p", so `TrimRight` finds no trailing dash and the result is the full `impl/abc-defghijklm-nop`. This case must also be updated (or repurposed to test that `TrimRight` still trims a slug that itself ends in "-" without any length cap involved) — treating it as in-scope of "the case ... must be updated/removed" since it exercises the same removed behavior.
- `ai-docs/spec/workflow-skills.md#L704-707` — full sentence: "the branch's naming convention gates the remaining flow: a branch named `impl/<stem>` (the convention `lead-implement` uses for branches it creates, `<stem>` hard-truncated to 15 characters with a trailing `-` trimmed) is deleted without asking." Only the parenthetical needs softening; the surrounding auto-delete-gate sentence structure (naming convention as trust boundary) stays intact since it's prefix-based, not length-based (see finding above).
- `ai-docs/spec/mcp-tools.md#L283-286` — full sentence: "Fresh implementation branches are created under the `impl/<stem>` convention, with `<stem>` hard-truncated to 15 characters (trailing `-` trimmed); legacy `implement/<scope-slug>` branches already in progress are still recognized as implementation branches for continue/rename purposes." Soften only the `<stem>` clause.
- `ai-docs/mental-model/workflow-skills.md#L48` — full sentence fragment: "normally creates `impl/<stem>` (stem capped at 15 characters) before source edits elsewhere". Soften the parenthetical only; rest of the sentence (rename/close-docs/merge behavior) is unrelated and must not change.

## Implementation Plan
1. `agents-plugin-tool/internal/mcp/implement_resolver.go#L672-684` — Remove the `if len(stem) > 15 { stem = stem[:15] }` block from `implementTargetBranchName`; keep `strings.TrimRight(stem, "-")` (still useful if a caller-supplied slug itself ends in `-`). Update the doc comment (L672-676) to describe advisory behavior, e.g.: "builds the canonical implementation branch name for a given scope slug: `impl/` followed by the slug (<=15 characters recommended, not enforced) with any trailing `-` trimmed." Do not touch the call sites at L687 (`deriveImplementBranchPlan`) or `session_state.go:1012` (`handleEnterImplement`) — both keep calling this helper unchanged, preserving the single-constructor invariant.
2. `agents-plugin-tool/internal/mcp/implement_resolver_test.go#L621-628` — Update `wantTargetBranch` for `"target branch name is truncated to 15 characters"` to the untruncated value `"impl/a-very-long-scope-slug-name"`; rename the case to reflect no-truncation (e.g. `"target branch name is not truncated when scope slug exceeds 15 characters"`).
3. `agents-plugin-tool/internal/mcp/implement_resolver_test.go#L629-636` — Update `"target branch truncation trims trailing dash"`: since `abc-defghijklm-nop` no longer truncates, either (a) change the fixture to a slug that itself ends in `-` after the scope slug logic (e.g. a `ScopeSlug` ending in `-`, if that can occur before this helper is called) so the case still exercises `TrimRight`, or (b) if no realistic caller ever passes a trailing-dash slug into this helper directly, remove this case and rely on case (2)'s untruncated assertion plus any existing dash-trim coverage. Prefer (a) if a minimal fixture change preserves trailing-dash-trim coverage; otherwise remove and note removal in the commit body. Rename whichever case remains to match actual behavior.
4. `ai-docs/spec/workflow-skills.md:706` — Replace "`<stem>` hard-truncated to 15 characters with a trailing `-` trimmed" with "`<stem>` <=15 characters recommended, with any trailing `-` trimmed". Leave L704-705 and L707+ unchanged.
5. `ai-docs/spec/mcp-tools.md:284` — Replace "with `<stem>` hard-truncated to 15 characters (trailing `-` trimmed)" with "with `<stem>` <=15 characters recommended (trailing `-` trimmed)". Leave the rest of the sentence (legacy `implement/<scope-slug>` recognition) unchanged.
6. `ai-docs/mental-model/workflow-skills.md:48` — Replace "(stem capped at 15 characters)" with "(stem <=15 characters recommended)". Leave the rest of the anchor-tagged bullet (`{#260505-implementation-workflow-skills} {#260707-impl-branch-convention-autodelete}`) unchanged.

## Verification Plan
- `go build ./...` from `agents-plugin-tool/` must pass.
- `go test ./internal/mcp/...` (or the project's standard invocation for that package) must pass, including the two updated/adjusted test cases.
- `grep -rn "len(stem) > 15\|hard-truncat" agents-plugin-tool/internal/mcp/` should return no remaining enforced-truncation call sites for the branch slug after the fix (the doc-comment wording itself will no longer contain "hard-truncat" once step 1 lands).
- Manual check: confirm `deriveImplementBranchPlan` (implement_resolver.go#L687) and `handleEnterImplement` (session_state.go#L1012) still both call `implementTargetBranchName` with no duplicated inline construction (no diff expected at either call site).

## Escalations
- None.
