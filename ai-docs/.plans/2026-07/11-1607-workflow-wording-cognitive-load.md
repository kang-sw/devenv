# Plan: workflow-wording-cognitive-load — Compress duplicated and defensive judgment wording without semantic change

## Relevant Ticket Contract
- Inline request only: reduce the model's reading and judgment load in `agents-plugin/rsrc/lead-implement/lead-implement.md#L47-L52` and `agents-plugin/rsrc/lead-proceed/lead-proceed.md#L23-L58` without changing routing, policy facts, safety gates, or ticket requirements.
- Preserve the settled boundary: a clear request to streamline implementation enables the low-ceremony preference; labels such as `hotfix`, `tweak`, or `small fix` alone do not.
- Preserve the settled ticket boundary: one bounded reviewable slice recoverable from linked spec/commit history may remain ticketless regardless of file count or public surface; multiple independently reviewable phases or otherwise unavailable durable pre-implementation traceability require a ticket.
- Verification must cover both full-ws and wsflow rendered playbooks, generated manifest/mirror consistency, and a fresh-reader check for semantic drift.

## Out of Scope
- `agents-plugin-tool/internal/mcp/implement_resolver.go` and `agents-plugin-tool/internal/mcp/proceed_resolver.go` — do not change deterministic verdict computation, schemas, warnings, or todo instructions.
- `ai-docs/spec/workflow-skills.md`, `ai-docs/spec/mcp-tools.md`, and mental models — no contract or domain-rule change is intended; update only if the wording pass exposes an actual contradiction rather than for prose synchronization.
- Delegation, survey planning, review allocation, documentation closeout, branch safety, merge, and push behavior.

## Codebase Findings
- `agents-plugin/rsrc/lead-implement/lead-implement.md#L42-L56` — the pre-call policy mapping is the required Layer 3 seam, but lines 50-51 repeat the same low-ceremony boundary defensively. Rewrite them exactly as: `Map a clear request to streamline implementation to policy.low_ceremony_if_safe=yes; labels such as hotfix, tweak, or small fix alone do not count.` and `This preference changes no other policy or gate.` Keep the explicit documentation-skip mapping and `explicit_direct_edit_request` rule unchanged; together they preserve independent policy axes without enumerating every unaffected gate.
- `agents-plugin/rsrc/lead-proceed/lead-proceed.md#L23-L24` — delete the carry-forward sentence. The handoff executes `lead-implement` inline in the active conversation, and `lead-implement` already gathers explicit caller policy; the sentence duplicates shared context and mentions branch-level mechanics unnecessarily.
- `agents-plugin/rsrc/lead-proceed/lead-proceed.md#L51-L58` — compress the four-row `needs-ticket` table to two rows: `Yes | Accepted work needs multiple independently reviewable phases or durable contract/verification traceability that linked spec/commit history cannot preserve before implementation` and `No | Accepted work is one bounded reviewable slice recoverable from linked spec/commit history, regardless of file count or public surface`. The preceding `discussion-needed` judgment already owns unresolved user decisions, so the internal-hygiene row and “after required discussion” qualifier are redundant here.
- `agents-plugin-tool/internal/mcp/playbook_tools_test.go#L1773-L1803` and `#L1885-L1891` — full-ws and wsflow golden assertions encode the verbose low-ceremony text. Replace them with assertions for the two concise sentences and retain the `hotfix`/`tweak`/`small fix` guard.
- `agents-plugin-tool/internal/mcp/playbook_tools_test.go#L1917-L1925` — remove the assertion for the deleted carry-forward sentence and add assertions for both compressed `needs-ticket` rows so the unchanged ticket boundary remains executable evidence.
- `agents-plugin-wsflow/rsrc/lead-implement/lead-implement.md` and `agents-plugin-wsflow/rsrc/lead-proceed/lead-proceed.md` — generated byte-identical mirrors; regenerate from canonical sources rather than editing directly. `agents-plugin/rsrc/manifest.json` and its wsflow copy also change because playbook hashes are content-addressed.

## Implementation Plan
1. Edit `agents-plugin/rsrc/lead-implement/lead-implement.md#L47-L52` with the two exact concise low-ceremony sentences above; retain the separate documentation-skip mapping and direct-edit rule.
2. Edit `agents-plugin/rsrc/lead-proceed/lead-proceed.md#L23-L58`: delete the redundant handoff carry sentence and replace the four `needs-ticket` rows with the two exact boundary rows above.
3. Update `agents-plugin-tool/internal/mcp/playbook_tools_test.go#L1773-L1925` so both product render tests assert the concise low-ceremony contract and the proceed test asserts the compressed ticket boundary, with no assertion retaining deleted prose.
4. From `agents-plugin-tool/`, regenerate `agents-plugin/rsrc/manifest.json` with `WSRSRC_REGEN=1 go test ./internal/wsrsrc/... -count=1 -run TestGenerateRealManifest`, then regenerate the byte-identical `agents-plugin-wsflow/rsrc/` tree with `WS_REGEN_WSFLOW_RSRC=1 go test ./internal/wsrsrc -count=1 -run TestRegenerateWsflowRsrcMirror`.
5. Run the required fresh-reader audit on both canonical playbooks and a downstream consistency sweep across render tests, specs, mental models, and wsflow mirrors; accept no finding that changes the settled behavior under the guise of compression.

## Verification Plan
- From `agents-plugin-tool/`: `go test ./internal/mcp ./internal/wsrsrc -count=1`.
- From repository root: `python3 -m unittest discover agents-plugin-wsflow/tests`.
- Confirm canonical/wsflow byte identity through `TestWsflowRsrcMirrorUpToDate` in the focused Go run and inspect `git diff --check`.
- Inspect the final diff against `fbc4e65b` (the behavioral source baseline) and confirm only prose, matching render assertions, generated manifests, and byte-identical mirrors changed; no MCP resolver or schema file changed.
- Count canonical playbook words/sentences before and after and report the reduction; duplicated wsflow copies are packaging size, not additional per-agent reading load.

## Escalations
- None.
