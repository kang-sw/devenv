---
kind: text
---
## Sample Conventions

This is a sample text dependency included by playbooks via the auto-include
mechanism. It demonstrates that rsrc include files are plain Markdown with
optional frontmatter, resolved by bare name at load time.
