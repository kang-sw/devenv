{
  "description": "Cross-session mailbox wake: a turn-boundary Stop hook that asks the model to drain unread mail when this session's own WS_MAILBOX inbox has any queued.",
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "python3 \"$PLUGIN_ROOT/bin/ws-mcp-launcher.py\" mailbox codex-stop-hook --slug \"${WS_MAILBOX:-}\"",
        "timeout": 10
      }
    ]
  }
}
