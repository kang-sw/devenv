{
  "description": "Cross-session mailbox wake: a turn-boundary Stop hook that asks the model to drain unread mail when this session's own WS_MAILBOX inbox has any queued. Claude Code auto-discovers this file by its hooks/hooks.json path; there is no plugin.json pointer to it (contrast the Codex adapter, whose manifest names its hooks file explicitly).",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/bin/ws-mcp-launcher.py\" mailbox claude-stop-hook || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
